
export interface AspectRatio {
  id: string;
  label: string;
  value: string;
}

export interface LightingStyle {
  id: string;
  label: string;
  description: string;
}

export interface CameraPerspective {
  id: string;
  label: string;
  description: string;
}
